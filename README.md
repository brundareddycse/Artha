# Artha — Financial Intelligence

AI-powered financial co-pilot for India. Built for ET AI Hackathon 2026.

## Files
- `index.html` — The complete app (React, all pages, everything bundled)
- `404.html` — Fixes page refresh on GitHub Pages


## Deploy to GitHub Pages

```bash
git init
git add .
git commit -m "Initial deploy"
git remote add origin Analyzing your answers...

Ran terminal command: git status 2>&1
Completed with input: git status 2>&1

Ran terminal command: git init ; git add . ; git commit -m "Initial commit: MoneyMind financial app with Claude API integration" 2>&1

Now let me add the remote and push to GitHub:  

Ran terminal command: git remote add origin https://github.com/brundareddycse/Artha.git ; git branch -M main ; git push -u origin main 2>&1

Perfect! ✅ Your project has been successfully pushed to GitHub!

**Summary:**
- ✅ Git repository initialized
- ✅ All 26 files committed (including .env.example and .gitignore)
- ✅ Pushed to: https://github.com/brundareddycse/Artha
- ✅ Main branch set up

**What's included:**
- MoneyMind React app with all components
- Claude API integration
- Environment configuration (.env.example for security)
- All utilities and contexts

**Note:** Your .env file with the Claude API key is protected by .gitignore and wasn't committed (for security).

Your project is now on GitHub and ready to go! 🚀
git branch -M main
git push -u origin main
```

Then go to repo Settings → Pages → Deploy from branch → main → Save.


